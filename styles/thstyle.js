import {StyleSheet} from 'react-native';

const thStyles = StyleSheet.create({

    wrapper: {
        marginLeft: 16,
        paddingTop: 30
    },

    header: {
        fontSize: 30, 
        fontWeight: 'bold',  
        marginLeft: 16, 
        paddingTop: 30, 
        color: '#1877F2', 
    },

    card: {
        backgroundColor: "#fff",
        margin: 8,
        borderRadius: 10,
        height: 450,
    },
})

export { thStyles }
